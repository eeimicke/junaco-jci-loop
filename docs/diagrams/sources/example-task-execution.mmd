flowchart TD
    Goal[PiF1o] -->|DECOMPOSES_INTO| Parent[COMPOSITE Task]
    Goal -->|DECOMPOSES_INTO| First[ATOMIC Task A]
    Goal -->|DECOMPOSES_INTO| Second[ATOMIC Task B]
    Parent -->|DECOMPOSES_INTO| First
    Parent -->|DECOMPOSES_INTO| Second
    Second -->|DEPENDS_ON| First
    Goal -->|DECOMPOSES_INTO| Prerequisite[ATOMIC Task prerequisite C]
    Parent -->|DEPENDS_ON| Prerequisite
    Parent -. "own prerequisite first; then current children" .-> Status[released Composite status]
    Parent -. joint completion graph must be acyclic .-> CycleCheck[DECOMPOSES_INTO and DEPENDS_ON]
